server:
  port: 8083

spring:
  application:
    name: reconciliation-poc
  datasource:
    url: jdbc:h2:mem:recon;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
