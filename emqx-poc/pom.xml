<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.0</version>
        <relativePath/>
    </parent>

    <groupId>com.claude.emqx</groupId>
    <artifactId>emqx-production-patterns-poc</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>EMQX Production Patterns POC</name>
    <description>Production-grade demos of the patterns that break in real EMQX/MQTT deployments.</description>

    <modules>
        <module>common</module>
        <module>01-million-connections</module>
        <module>02-connection-storm</module>
        <module>03-qos-levels</module>
        <module>04-shared-subscriptions</module>
        <module>05-auth-jwt-mtls</module>
        <module>06-rule-engine-kafka-bridge</module>
        <module>07-mqtt5-features</module>
        <module>08-lwt-presence</module>
        <module>09-session-persistence</module>
        <module>10-retained-messages</module>
        <module>11-device-shadow</module>
        <module>12-sparkplug-b</module>
        <module>13-ota-updates</module>
        <module>14-cluster-split-brain</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Eclipse Paho is the de-facto Java MQTT client; v1.2.5 supports MQTT 5 -->
        <paho.version>1.2.5</paho.version>
        <!-- HiveMQ MQTT client - alternative, fully async, MQTT 5 first-class -->
        <hivemq.version>1.3.3</hivemq.version>
        <!-- Kafka for bridge POC -->
        <kafka.version>3.9.0</kafka.version>
        <spring-kafka.version>3.3.0</spring-kafka.version>
        <!-- JWT for auth POC -->
        <jjwt.version>0.12.6</jjwt.version>
        <!-- Sparkplug B protobuf -->
        <protobuf.version>3.25.5</protobuf.version>
        <!-- Bouncy Castle for cert generation in mTLS POC -->
        <bouncycastle.version>1.78.1</bouncycastle.version>
        <!-- Resilience4j for jittered retry in storm POC -->
        <resilience4j.version>2.2.0</resilience4j.version>
        <!-- Testcontainers -->
        <testcontainers.version>1.20.3</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.claude.emqx</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Paho MQTT client (sync + async, MQTT 3.1.1 and 5.0) -->
            <dependency>
                <groupId>org.eclipse.paho</groupId>
                <artifactId>org.eclipse.paho.mqttv5.client</artifactId>
                <version>${paho.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.paho</groupId>
                <artifactId>org.eclipse.paho.client.mqttv3</artifactId>
                <version>${paho.version}</version>
            </dependency>

            <!-- HiveMQ client (reactive, MQTT 5 first-class) -->
            <dependency>
                <groupId>com.hivemq</groupId>
                <artifactId>hivemq-mqtt-client</artifactId>
                <version>${hivemq.version}</version>
            </dependency>
            <dependency>
                <groupId>com.hivemq</groupId>
                <artifactId>hivemq-mqtt-client-reactor</artifactId>
                <version>${hivemq.version}</version>
            </dependency>

            <!-- Spring Integration MQTT for declarative subscribers -->
            <dependency>
                <groupId>org.springframework.integration</groupId>
                <artifactId>spring-integration-mqtt</artifactId>
            </dependency>

            <!-- Kafka -->
            <dependency>
                <groupId>org.apache.kafka</groupId>
                <artifactId>kafka-clients</artifactId>
                <version>${kafka.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.kafka</groupId>
                <artifactId>spring-kafka</artifactId>
                <version>${spring-kafka.version}</version>
            </dependency>

            <!-- JWT (for auth POC) -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- Protobuf for Sparkplug B -->
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>${protobuf.version}</version>
            </dependency>

            <!-- Bouncy Castle for X.509 cert generation -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcpkix-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- Resilience4j -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>

            <!-- Testcontainers -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
